<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    </style>

    <!-- In app/src/main/res/values/styles.xml -->
    <style name="Widget.App.QuantitiesButton" parent="Widget.Material3.Button">
        <!-- ... other items like textSize, etc. -->

        <!-- This is the new, important line -->
        <item name="backgroundTint">@color/button_quantities_selector</item>
    </style>Merge

    <style name="Widget.App.QuantityButton" parent="Widget.Material3.Button">
        <!-- Make the text larger and more legible for a dialog -->
        <item name="android:textSize">22sp</item>

        <!-- Increase the vertical padding to make the buttons taller and easier to tap -->
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>

        <!-- Make the text bold to stand out -->
        <item name="android:textStyle">bold</item>

        <!--
           Optional: Set the corner radius. Here we use Material 3's "Full"
           style, which makes the button a pill shape. This is great for
           prominent, distinct actions.
        -->
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.Material3.Corner.Full</item>
    </style>

    <!-- Order Table Header Style -->
    <style name="OrderTableHeader">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">16dp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@color/purple_700</item>
        <item name="android:textStyle">bold</item>
        <item name="android:elevation">4dp</item>
    </style>

    <!-- Order Total Price Style (Yellow text on purple background) -->
    <style name="OrderTotalPrice">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#FFFF00</item> <!-- Yellow -->
        <item name="android:background">#800080</item> <!-- Purple -->
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:gravity">end|center_vertical</item>
        <item name="android:textStyle">bold</item>
        <item name="android:elevation">8dp</item>
        <item name="android:letterSpacing">0.05</item>
    </style>

    <!-- Button Style -->
    <style name="OrderButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="backgroundTint">@color/purple_500</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:layout_margin">4dp</item>
    </style>

    <!-- RecyclerView Item Style -->
    <style name="OrderItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:background">?attr/selectableItemBackground</item>
    </style>

    <!-- Menu Page Button Style -->
    <style name="MenuPageButton">
        <item name="android:layout_width">64dp</item>
        <item name="android:layout_height">64dp</item>
        <item name="android:background">@drawable/vloer11</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">14sp</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Bottom Action Bar Style -->
    <style name="BottomActionBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:background">@color/purple_200</item>
        <item name="android:elevation">8dp</item>
    </style>

    <!--
  Custom ButtonBarButtonStyle with a background color.
  It inherits the borderless look and feel from the parent
  and applies our custom background on top.
-->
    <style name="Widget.App.Button.KassakuButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:backgroundTint">@color/ANDROID_BUTTON_BACKGROUND</item>
        <item name="android:textColor">@color/ANDROID_BUTTON_TEXT</item>
        <!-- Optional: You can also set text color or other attributes here -->
        <!-- <item name="android:textColor">@color/your_text_color</item> -->
    </style>
</resources>