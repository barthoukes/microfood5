<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/ANDROID_BUTTON_BACKGROUND"
    android:padding="16dp">

    <!-- Horizontal container for icon, name, and amount -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Icon -->
        <ImageView
            android:id="@+id/payment_icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="16dp"
            android:src="@drawable/cancel"
            android:tint="#FF6200EE"
            tools:src="@drawable/pay_cash40" />

        <!-- Name -->
        <TextView
            android:id="@+id/floor_table_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:textColor="@color/ANDROID_BUTTON_TEXT"
            android:textSize="20sp"
            tools:text="Cash" />

        <!-- Amount -->
        <TextView
            android:id="@+id/floor_table_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/ANDROID_BUTTON_TEXT"
            android:textSize="20sp"
            android:textStyle="bold"
            tools:text="$100.00" />
    </LinearLayout>

    <!-- Divider -->
    <View
        android:id="@+id/dividerView"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="16dp"
        android:background="#E0E0E0"
        android:visibility="gone" />
</LinearLayout>